INIT Init
NEXT Next

CONSTANTS
    \* SdkMsg <- instance_SdkMsg
    \* Seq <- BSeq
    \* Real <- instance_Numbers
    CoinAmount <- instance_Numbers
    \* DENOM <- instance_DENOM
    MinDeposit <- instance_MinDeposit
    GovModuleAccountAddress <- instance_GovModuleAccountAddress

INVARIANTS
    TypeOK

CHECK_DEADLOCK FALSE
